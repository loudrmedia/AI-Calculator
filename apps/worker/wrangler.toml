name = "ai-calculator-api"
main = "src/index.ts"
compatibility_date = "2024-05-12"

[vars]
ENVIRONMENT = "production"

# Rate limiting configuration
# [unsafe]
# bindings = [
#   { name = "RATE_LIMITER", class_name = "RateLimiter" }
# ]

# Secrets (set via wrangler secret put):
# - ZAPIER_WEBHOOK_URL
# - WEBHOOK_SECRET
