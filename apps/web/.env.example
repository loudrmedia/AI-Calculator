# API Configuration
# For local development, leave empty to use /api/lead
# For production with Cloudflare Worker, set to your worker URL
NEXT_PUBLIC_WORKER_URL=https://api.getautoreliefassistance.com/api/lead

# The Worker endpoint receives lead submissions and forwards to Zapier
